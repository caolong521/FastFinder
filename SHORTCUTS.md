# FastFinder 快捷键说明

FastFinder 的目标是让“呼出 → 输入 → 打开 → 收起”尽量只靠键盘完成。所有全局快捷键都可以在 **设置** 中自定义。

## 1. 全局快捷键

### 主窗口显示 / 隐藏

默认：

```text
Ctrl + Alt + Space
```

- FastFinder 隐藏时：显示主窗口并自动聚焦搜索框。
- FastFinder 主窗口处于前台时：再次按下会隐藏到系统托盘。
- 在浏览器、VS Code、Visual Studio、资源管理器等其它程序中也可使用。

### 小搜索窗

默认：

```text
Ctrl + Alt + F
```

作用：

```text
Ctrl + Alt + F
        ↓
屏幕中央出现小搜索窗
        ↓
输入关键词
        ↓
↑ / ↓ 选择结果
        ↓
Enter 打开
        ↓
窗口自动隐藏
```

小搜索窗中：

| 快捷键 | 功能 |
| --- | --- |
| `Enter` | 打开当前结果并隐藏小搜索窗 |
| `Ctrl + Enter` | 在资源管理器中定位当前结果 |
| `Esc` | 隐藏小搜索窗 |

> 主窗口快捷键和小搜索窗快捷键不能设置为同一个组合。如果快捷键已被 Windows 或其它软件占用，FastFinder 会提示更换。

## 2. 主窗口快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl + L` | 聚焦搜索框 |
| `Ctrl + F` | 聚焦搜索框 |
| `Esc` | 搜索框有文字时先清空；为空时可隐藏到托盘 |
| `Enter` | 打开当前选中的文件或文件夹 |
| `Ctrl + Enter` | 在 Windows 资源管理器中定位当前结果 |
| `Ctrl + C` | 复制当前结果的完整路径 |
| `Ctrl + Shift + C` | 复制当前结果的文件名/文件夹名 |
| `Ctrl + I` | 打开长期索引库 |
| `Ctrl + H` | 打开搜索历史 |
| `Ctrl + B` | 打开收藏 |
| `Ctrl + ,` | 打开 FastFinder 设置 |
| `Alt + 1` | 切换到“索引搜索” |
| `Alt + 2` | 切换到“指定目录”临时搜索 |

## 3. 鼠标拖拽

搜索结果支持直接拖出 FastFinder：

```text
搜索结果
   ↓ 拖动
Windows 桌面 / 资源管理器 / 支持文件拖入的软件
```

拖出操作使用 Windows 文件 URL，不会修改 FastFinder 索引。

## 4. 托盘操作

- 双击托盘 FastFinder 图标：显示主窗口。
- 托盘菜单可以直接打开“小搜索窗”。
- 可以打开收藏、搜索历史、索引库和设置。
- 点击主窗口右上角 `X` 时，如果启用了“关闭到托盘”，程序仍会在后台维护索引。

## 5. Windows 开机自启

开机自启 **默认关闭**。

只有在：

```text
设置 → Windows 开机自启
```

主动勾选并保存后，FastFinder 才会写入当前 Windows 用户的启动项。

FastFinder 使用 `HKEY_CURRENT_USER` 用户级启动项，不写系统级 `HKEY_LOCAL_MACHINE`，正常情况下不需要管理员权限。如果公司组策略或安全软件禁止修改启动项，程序会提示失败，不会强制绕过限制。

## 6. 推荐使用方式

完整搜索：

```text
Ctrl + Alt + Space
        ↓
输入文件名 / 文件夹名
        ↓
↑ / ↓ 选择结果
        ↓
Enter 打开
```

高频快速搜索：

```text
Ctrl + Alt + F
        ↓
输入关键词
        ↓
Enter
```

---

源码运行时，本文件位于项目根目录 `SHORTCUTS.md`。如果后续使用 PyInstaller 打包，请把它作为数据文件一起打包。
