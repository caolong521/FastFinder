# FastFinder 索引存档管理 + 快捷键说明补丁

## 新增

- `SHORTCUTS.md`：完整快捷键说明。
- 设置窗口增加“查看快捷键说明”按钮。
- 长期索引库增加“清空索引数据”。
- 删除长期索引存档改为两次确认。

## 清空与删除的区别

### 清空索引数据

只删除所选目录在 SQLite 中已经写入的文件/文件夹索引记录，保留该目录配置。
清空后会自动暂停该目录，避免后台立即重新建立索引。
以后重新勾选启用并点击“重新扫描”即可恢复。

### 删除所选存档

删除所选目录的索引数据，同时删除该目录在 FastFinder 中的长期索引配置。
此操作需要连续两次确认。

两种操作都不会删除磁盘上的真实文件或文件夹。
